# Local MongoDB Connection String (Points to local MongoDB service)
MONGODB_URI=mongodb://localhost:27017/codedpad

# For Vercel Blob cloud file uploads, this token will be set automatically in production on Vercel.
# For local file uploads testing, leave this empty to save uploads to the local uploads directory.
BLOB_READ_WRITE_TOKEN=
